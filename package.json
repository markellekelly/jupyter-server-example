{
  "name": "myext",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc -p src && webpack",
    "clean": "rimraf build",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "webpack": "~5.75.0",
    "webpack-cli": "^5.0.1",
    "whatwg-fetch": "~2.0.3"
  }
}
